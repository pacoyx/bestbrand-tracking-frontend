import { PrependZeroOptionPipe } from './prepend-zero-option.pipe';

describe('PrependZeroOptionPipe', () => {
  it('create an instance', () => {
    const pipe = new PrependZeroOptionPipe();
    expect(pipe).toBeTruthy();
  });
});
